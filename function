
from fractions import Fraction
from functools import reduce

def product(fracs):
    # reduce multiplies all Fraction objects in the list
    t = reduce(lambda x, y: x * y, fracs)
    return t.numerator, t.denominator


